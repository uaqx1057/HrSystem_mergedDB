@extends('layouts.app')
@section('content')<div class="content-wrapper"><div class="card"><div class="card-body"><h4>Required certification rules</h4><form method="POST" action="{{ route('hr-certification-rules.store') }}">@csrf<select name="designation_id"><option value="">All designations</option>@foreach($designations as $designation)<option value="{{ $designation->id }}">{{ $designation->name }}</option>@endforeach</select><input name="certification_name" placeholder="Certification" required><input name="renewal_months" type="number" placeholder="Renew every months"><button class="btn btn-primary">Save rule</button></form><table class="table mt-3"><thead><tr><th>Designation</th><th>Certification</th><th>Renewal</th></tr></thead><tbody>@foreach($rules as $rule)<tr><td>{{ $rule->designation?->name ?? 'All' }}</td><td>{{ $rule->certification_name }}</td><td>{{ $rule->renewal_months ?? 'Manual' }}</td></tr>@endforeach</tbody></table></div></div></div>@endsection
