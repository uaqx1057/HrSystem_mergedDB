@extends('layouts.app')
@section('content')
<div class="content-wrapper"><div class="d-flex justify-content-between mb-2"><h4>HR compliance</h4><a class="btn btn-light" href="{{ route('hr-certification-rules.index') }}">Certification rules</a></div><div class="card"><div class="card-body">
<form method="POST" action="{{ route('hr-compliance.probation') }}">@csrf<select name="employee_id">@foreach($employees as $e)<option value="{{ $e->id }}">{{ $e->name }}</option>@endforeach</select><select name="review_day"><option>30</option><option>60</option><option>90</option></select><input type="date" name="due_date" required><button>Save probation review</button></form><hr>
<form method="POST" action="{{ route('hr-compliance.certification') }}">@csrf<select name="employee_id">@foreach($employees as $e)<option value="{{ $e->id }}">{{ $e->name }}</option>@endforeach</select><input name="name" placeholder="Certification" required><input type="date" name="expires_at"><button>Save certification</button></form><hr>
<form method="POST" action="{{ route('hr-compliance.case') }}">@csrf<input name="category" placeholder="Restricted case category" required><input name="details" placeholder="Confidential details" required><button>Create restricted case</button></form></div></div>
<div class="card mt-3"><div class="card-body"><h5>Probation reviews</h5>@foreach($reviews as $r)<div>{{ $r->employee->name }} — day {{ $r->review_day }} — {{ $r->status }}<form class="d-inline" method="POST" action="{{ route('hr-compliance.probation.complete',$r) }}">@csrf<select name="status"><option value="confirmed">Confirm</option><option value="extended">Extend</option><option value="completed">Complete</option></select><input name="notes" placeholder="Review notes"><button>Save</button></form></div>@endforeach<h5 class="mt-3">Certifications</h5>@foreach($certifications as $c)<div>{{ $c->employee->name }} — {{ $c->name }} — {{ $c->expires_at?->format('Y-m-d') }}<form class="d-inline" method="POST" action="{{ route('hr-compliance.certification.renew',$c) }}">@csrf<input type="date" name="expires_at" required><button>Renew</button></form></div>@endforeach<h5 class="mt-3">Restricted cases</h5>@foreach($cases as $case)<div>{{ $case->category }} — {{ $case->status }}<form class="d-inline" method="POST" action="{{ route('hr-compliance.case.update',$case) }}">@csrf<select name="status"><option>open</option><option>in_review</option><option>closed</option></select><button>Update</button></form></div>@endforeach</div></div></div>
@endsection
