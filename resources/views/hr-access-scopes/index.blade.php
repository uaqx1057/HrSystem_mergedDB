@extends('layouts.app')
@section('content')
<div class="content-wrapper"><div class="card mb-3"><div class="card-body"><h4>Head Office HR cross-branch access</h4><p class="text-muted">This is HR-only. It does not change DMS or DOBS roles.</p><form method="POST" action="{{ route('hr-access-scopes.store') }}" class="form-inline">@csrf<select class="form-control mr-2" name="user_id" required>@foreach($employees as $employee)<option value="{{ $employee->id }}">{{ $employee->name }}</option>@endforeach</select><select class="form-control mr-2" name="module" required>@foreach($modules as $module)<option value="{{ $module }}">{{ str_replace('_', ' ', $module) }}</option>@endforeach</select><input class="form-control mr-2" type="date" name="ends_at"><button class="btn btn-primary">Grant</button></form></div></div><div class="card"><div class="card-body"><table class="table"><thead><tr><th>User</th><th>Module</th><th>Status</th><th>Expiry</th><th></th></tr></thead><tbody>@forelse($scopes as $scope)<tr><td>{{ $scope->user?->name }}</td><td>{{ str_replace('_', ' ', $scope->module) }}</td><td>{{ $scope->is_active ? 'Active' : 'Revoked' }}</td><td>{{ optional($scope->ends_at)->format('Y-m-d') }}</td><td>@if($scope->is_active)<form method="POST" action="{{ route('hr-access-scopes.revoke', $scope) }}">@csrf<button class="btn btn-sm btn-danger">Revoke</button></form>@endif</td></tr>@empty<tr><td colspan="5">No explicit cross-branch grants.</td></tr>@endforelse</tbody></table></div></div></div>
@endsection
