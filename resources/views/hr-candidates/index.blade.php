@extends('layouts.app')
@section('content')<div class="content-wrapper"><div class="card"><div class="card-body"><h4>Recruitment candidates</h4><form method="POST" action="{{ route('hr-candidates.store') }}" class="form-row">@csrf<input class="form-control col" name="name" placeholder="Name" required><input class="form-control col" name="email" placeholder="Email"><input class="form-control col" name="mobile" placeholder="Mobile"><select class="form-control col" name="branch_id"><option value="">Branch</option>@foreach($branches as $b)<option value="{{ $b->id }}">{{ $b->name }}</option>@endforeach</select><input class="form-control col" name="designation" placeholder="Designation"><button class="btn btn-primary">Stage candidate</button></form></div></div><div class="card mt-3"><div class="card-body"><table class="table"><thead><tr><th>Name</th><th>Contact</th><th>Status</th><th></th></tr></thead><tbody>@foreach($candidates as $c)<tr><td>{{ $c->name }}</td><td>{{ $c->email }} {{ $c->mobile }}</td><td>{{ $c->status }}</td><td>@if($c->status!=='converted')<form method="POST" action="{{ route('hr-candidates.handoff',$c) }}">@csrf<button class="btn btn-sm btn-primary">Create employee</button></form>@endif</td></tr>@endforeach</tbody></table></div></div></div>@endsection
