@extends('layouts.app')
@section('content')
<div class="content-wrapper"><div class="card"><div class="card-body"><h4>Delegate leave approvals</h4><p class="text-muted">Delegates can act only for your direct reports during the selected dates. This never gives branch-wide access.</p>
@if($isManager)<form method="POST" action="{{ route('hr-leave-delegations.store') }}" class="form-row">@csrf
<div class="col-md-4"><select name="delegate_id" class="form-control" required><option value="">Select delegate</option>@foreach($employees as $employee)<option value="{{ $employee->id }}">{{ $employee->name }}</option>@endforeach</select></div><div class="col-md-3"><input type="date" name="starts_at" class="form-control" required></div><div class="col-md-3"><input type="date" name="ends_at" class="form-control" required></div><div class="col-md-2"><button class="btn btn-primary">Save</button></div></form>@else<div class="alert alert-info">Only managers with direct reports can delegate approvals.</div>@endif
</div></div><div class="card mt-3"><div class="card-body"><h5>My delegations</h5><table class="table"><thead><tr><th>Delegate</th><th>Start</th><th>End</th><th>Status</th><th></th></tr></thead><tbody>@forelse($delegations as $delegation)<tr><td>{{ $delegation->delegate?->name }}</td><td>{{ $delegation->starts_at->format('Y-m-d') }}</td><td>{{ $delegation->ends_at->format('Y-m-d') }}</td><td>{{ $delegation->is_active ? 'Active' : 'Revoked' }}</td><td>@if($delegation->is_active)<form method="POST" action="{{ route('hr-leave-delegations.revoke', $delegation) }}">@csrf<button class="btn btn-sm btn-outline-danger">Revoke</button></form>@endif</td></tr>@empty<tr><td colspan="5">No delegations created.</td></tr>@endforelse</tbody></table></div></div></div>
@endsection
